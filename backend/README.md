# Node.js Backend

This is the backend for the project, built with Express.js.

## Getting Started

1. Install dependencies:
   ```sh
   npm install
   ```
2. Start the server:
   ```sh
   node index.js
   ```

The server will run on http://localhost:3001/
